import {SideBarContainer} from './SideBarStyle';
import Nav from '../../elements/Nav/Nav';

const SideBar = () => {
  return (
    <SideBarContainer>
      <Nav />
    </SideBarContainer>
  );
};

export default SideBar;
